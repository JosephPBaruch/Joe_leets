func addBinary(a string, b string) string {
    var answer string
    var length int
    if len(a) > len(b) {
        length = len(a) + 1
    }else{
        length = len(b) + 1
    }
    for i := 0; i < length; i++ { // loop through every character from right to left
        if len(a)
        
            // if it doesnt exist -> 0
            // add first char of both together
            // if 0 then answer[i] = 0
            // if 1 then answer[i] = 1
            // if 2 then answer[i] = 0 and remainder is set 
    }
    
    return answer
}